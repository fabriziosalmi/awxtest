---

- hosts: all
  tasks:
    - name: site | hello world
      shell: echo "Hi! Ansible is working"
    - name: Execute the script
      command: ip a
      register: out
    - debug: var=out.stdout_lines
    - name: Test 2
      command: echo $hostname
      register: log2
    - debug: var=log2.stdout_lines
